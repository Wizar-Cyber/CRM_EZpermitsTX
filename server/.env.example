# Server
PORT=4000
JWT_SECRET=change_me

# PostgreSQL (recommended for Drizzle)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME

# Optional: individual params (used by server/db.js)
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=5432
DB_NAME=
